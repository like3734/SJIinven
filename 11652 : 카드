package baekjoon;

import java.util.*;
import java.io.*;
import java.math.*;
import java.lang.Math;

public class Main{
    static long [] itemList;
    static long temp;
    static int result;
// Main
    public static void main(String[] args) throws Exception {
    	Scanner sc = new Scanner(System.in);
    	
    	int n = sc.nextInt();
    	itemList = new long[n];
    	for(int i=0; i<n; i++) {
    		itemList[i] = sc.nextLong();
    	}
    	Arrays.sort(itemList);
    	temp = itemList[0];
    	int count = 1, maxCount = 0; long result = temp;
    	for(int i=1; i<n;i++) {
    		if(temp == itemList[i]) {
    			count++;
    			if( i == n-1 ) {
    				if( maxCount < count ) {
        				result = itemList[i-1];
        			}
    			}
    		}
    		else {
    			if( maxCount < count ) {
    				maxCount = count;
    				result = itemList[i-1];
    			}
    			temp = itemList[i];
    			count = 1;
    		}
    	}
        
    	System.out.println(result);
    }
    
}

